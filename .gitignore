node_modules/
dist/
target/
src-tauri/target/
.DS_Store
*.log

# Local MCP server registration holds the forge sidecar admin bearer — never
# commit. Share .mcp.json.example (placeholder) instead.
.mcp.json

# mutation testing output
mutants.out/
mutants.out.old/

# local save slots (ticket #28) — player data, never source
saves/

# raw art packs (original/purchased tilesets + extracted working trees). The
# committed, sliced tile sheets live under public/tilesets/; raw_assets/ is
# scratch, never source.
raw_assets/
